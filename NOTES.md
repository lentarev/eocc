# NOTES

### OpenGL glad ext.
GL_MAX_TEXTURE_MAX_ANISOTROPY_EXT => GL_EXT_texture_filter_anisotropic

### Frequently used commands
```bash
# Show project structure up to 2 levels
tree -L 2

# Exclude service folders (for example, build, .git):
tree -I 'build|.git|\*.o'
```